# Generated by scripts/build-docs.ts from the API's /openapi.json — a build
# artefact, not a source file. `pnpm build` regenerates it.
docs.html

# JPM geometry bench — the Python pipeline, checkpoints and reference renders
# behind the jpm beat's structure. Local-only and large (~19MB of PNGs), but
# it IS the source of truth for iterating on that geometry: edit jpm_proto.py
# there, then port into src/scene/jpm-scene.ts. scratch/jpm/README.md documents
# the Figma registration chain the footprint was derived from.
scratch/
